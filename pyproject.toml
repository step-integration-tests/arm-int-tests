[project]
name = "widgets"
version = "0.1.0"
description = "Sample project for testing Dependabot uv ecosystem support"
requires-python = ">=3.9"
dependencies = [
    "requests==2.28.0",
    "click==8.1.0",
]

[dependency-groups]
dev = [
    "pytest==7.2.0",
]
